"""Canonical fixture tenant (TD §17.2).

One fixture tenant shared by every conformance family, so that a change to it
is reviewable and cannot silently suppress a previously passing case.

Not built by EWP-000.1A: it requires the schema, which a later work package owns.
"""
