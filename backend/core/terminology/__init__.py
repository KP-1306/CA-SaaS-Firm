"""Terminology projection.

Owns per-tenant vocabulary resolution applied at every rendering edge, so that
the domain keeps a fixed internal vocabulary (AR ADR-013, TD §12.9).

Not implemented by EWP-000.1A. This package exists because the frozen structure
requires it.
"""
