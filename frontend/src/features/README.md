# Features

One folder per domain feature: assignments, workflow editor, clients,
generation, dashboards and so on.

Empty by design. Every feature here is owned by the work package that
implements the corresponding bounded context.
