# Design system

Design tokens and accessible primitives for per-tenant branding.

Branding is applied through tokens, never free-form style injection, which
would be a cross-site scripting vector (TD §12.7). Status must never be
communicated by colour alone: every status carries a text label and an icon
(BL §11.8).

Empty by design. Owned by a later work package.
