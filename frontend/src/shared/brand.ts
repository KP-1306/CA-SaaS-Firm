/**
 * Central brand fallback for pre-authentication / shared surfaces that cannot
 * yet resolve the tenant FirmProfile. Feature surfaces should use the backend
 * brand resolution (features/console/branding.ts::useBrand) instead of this.
 */
export const DEFAULT_BRAND_NAME = 'Vridhi Consultants';
